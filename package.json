{
  "name": "justice_org",
  "version": "1.0.0",
  "description": "Full stack project with backend and frontend",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm run dev --prefix backend\" \"npm start --prefix frontend\"",
    "install-all": "npm install --prefix backend && npm install --prefix frontend"
  },
  "dependencies": {
    "concurrently": "^9.0.0"
  }
}
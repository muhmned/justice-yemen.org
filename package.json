{
  "name": "justice-org-monorepo",
  "version": "1.0.0",
  "description": "Root package.json for Justice Organization project",
  "main": "index.js",
  "scripts": {
    "install-backend": "npm install --prefix backend",
    "install-frontend": "npm install --prefix backend/frontend --legacy-peer-deps",
    "install": "npm run install-backend && npm run install-frontend",
    "build": "npm run build --prefix backend/frontend",
    "start": "npm start --prefix backend",
    "dev": "concurrently \"npm run start --prefix backend/frontend\" \"npm run dev --prefix backend\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@eslint/js": "^9.33.0",
    "concurrently": "^7.6.0"
  }
}
